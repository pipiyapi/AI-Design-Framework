# Tutorial intake routes

Use platform capture, signed-browser capture, local-media extraction, or explicit partial evidence. Keep source layers separate and record missing evidence instead of inventing coverage.

Write the result directly to `07-Workflows` as one two-part Chinese note: `原帖实现信息` plus `Workflow SOP`. Store 3–6 keyframes and optionally one 3–8 second preview up to 5 MB. Do not use Inbox approval or promotion for tutorials.
